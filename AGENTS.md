# 通用桌宠工作流

## 项目目标

- 建立一个不依赖 Codex、Petdex 或 VPN 的跨平台桌宠运行内核。
- 以同一套内核支持 Petdex v1/v2 宠物移植和单宠独立安装包导出。
- 最后接入“客户提供至少一张清晰完整全身正面图”的素材制作流程。
- 当前只制作内部测试版和候选版；商业签名、收款、DRM、商城和正式发布体系不在当前范围。

## 执行边界

- `Wukong-Windows-Desktop-Pet-Handoff-20260731/` 是只读参考基线，不覆盖、不重排、不清理其中源码、安装包、证据或批准素材。
- `~/.codex/pets/` 与 `~/.petdex/pets/` 中的原始宠物不得被转换流程原地修改；导入时创建带来源和哈希的新副本。
- 未明确商业授权的 Petdex 宠物仅用于本地兼容测试，不生成对外发布结论。
- 新产物使用新的、版本化且不覆盖的目录。
- 未经用户批准，不安装、卸载或替换系统应用，不修改系统 DPI、开机启动或安全设置。

## 兼容与质量门

- 支持 v1：8 列 × 9 行、192×208 单格、1536×1872 图集。
- 支持 v2：8 列 × 11 行、192×208 单格、1536×2288 图集。
- 宠物包必须记录原始来源、`pet.json`、图集路径、版本识别、SHA-256 和授权状态。
- 运行器必须从统一宠物包读取身份、图集和状态映射，不允许把 Wukong 名称、路径或动作写死在核心模块中。
- 保持 Electron API 在 main process，preload 只暴露最小且校验过的桥，renderer 不获得 Node 权限。
- 自动测试通过不能代替真实透明窗口和动态动作检查。
- macOS 结果只作为候选验证；Windows 正式验收必须区分 source、win-unpacked、installed mode，并在需要时覆盖 100%/125%/150% DPI。

## 当前阶段

- Phase 0：基线、PRD、样本和任务分解。
- Phase 1：共享桌宠运行内核。
- Phase 2：Petdex v1/v2 导入与校验工作流。
- Phase 3：单宠独立安装包工厂。
- Phase 4：客户素材制作与视觉确认工作流。
- 商业发布体系暂缓，不作为当前 Phase 5 待办。

每个阶段开始前确认范围；阶段结束时更新 `MEMORY/`、`tasks/todo.md` 和验证证据。
