# Mac 制作流程打磨（2026-09-12）

用户决定：先打磨升级，再统一验收；当前无 Windows 环境。基线 `472fe06`，开发分支 `fix/mac-studio-polish`。

本轮已完成：

- 按项目保留本次会话的配置草稿，支持撤销；保存失败保留输入，未保存配置阻止导出。
- 导入、保存、预览开关、导出等操作期间锁定项目切换；检查、导入、保存、预览和导出连接异常有可恢复提示。
- 按项目隔离检查／预览状态；同一项目切换步骤保留导出结果，隐藏页面停止点阵绘制。
- 动作检查增加攀爬入口、暂停、逐帧、0.25×–2× 速度和朝向；保持桌宠默认播放逻辑与原有蓝图设计。
- Shimeji ZIP 保留来源文件名，避免临时目录名进入角色身份；新项目默认宠物名限制在配置允许长度，完整项目名和来源不变。
- 源码环境导出前刷新运行时，禁止旧 out-pet 静默进入新包；打包环境继续使用附带构建。

开发回归已通过：`npm test`、`npm run typecheck`、工作台／运行时构建、41 项真实 Electron 桌宠冒烟。Doraemon v1 WebP、Boba v2 WebP、Mario Shimeji ZIP 的实际导入／预览／导出等 77 项检查通过；三份 ZIP 各 34 项静态核验通过，每份 10 个运行时文件与当前构建逐字节一致。实际使用图集帧未发现全透明空帧；浅色、深色及 820×560 窗口检查通过。测试只使用隔离 userData，原包和旧 deliverables 保留。

产物与日志：`deliverables/mac-studio-polish-20260912-2fr79Y/`。均为内部开发回归，三个角色的 ZIP 均保持 `internal-test-only`。早期回归曾复现草稿丢失、异常无提示、长名称默认配置非法和旧运行时缓存；最终结果以上述目录为准。

复跑：`npm run smoke:studio` 使用仓库自有 fixture 和隔离 userData，不触碰日常项目、不默认导出。可通过 `STUDIO_SMOKE_SAMPLES` 传入三个本地样本的 JSON 路径数组；`STUDIO_SMOKE_EXPORT=1` 额外实际导出。图像解码与绘制等待、故障注入和应用版本均由测试启动器控制。

尚未开展：正式统一验收、Windows 实机／DPI／窗口互动／长稳验收。批量队列与角色展示素材生产属于后续增量，本轮未实现。经典 XML 的既有兼容范围不扩张，未承诺完整 Shimeji 行为兼容。
