# Windows 执行提示词

严格执行 `phase-6-workbench-windows-acceptance-prd.md`。不要修改源码、永久 npm 配置、代理、安全设置或系统缩放之外的系统状态。每个门保存日志和截图；失败立即保留现场，不删除证据、不弱化测试。最终生成 `WINDOWS-WORKBENCH-REPORT.md`、`returned-checksums.sha256` 和完整 `RETURN/`。结束前恢复原系统缩放并确认工作台与预览桌宠进程数为 0。
