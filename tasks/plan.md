# PRD 与实施计划：通用桌宠工作流

## 问题

Petdex Desktop 可以脱离 Codex 运行，但下载和社区依赖网络环境。当前 Wukong 成品证明独立 Electron 桌宠可行，却把角色、图集和产品配置写死。项目需要把这套经验转化为离线、配置驱动、可重复生产多个桌宠安装包的工作流。

## 成功标准

- 同一运行内核可加载 Wukong、v1 `doraemon` 以及 v2 `dai`、`jokebear-codexpet`，核心代码不因换宠物而修改。
- 安装后的宠物不依赖 Codex、Petdex、VPN、账户或运行时网络。
- 导入器能够识别 v1/v2、校验输入、保存来源与哈希，并拒绝结构不完整或不安全的包。
- 每只宠物可通过产品配置生成独立名称、图标、appId/bundleId、版本和数据目录。
- Windows 与 macOS 构建和验收结论分开记录；未签名产物只称测试版或候选版。
- 客户素材流程最终支持至少一张清晰、完整、无遮挡的全身正面图，允许 AI 辅助，并在角色基准、动作、注视方向和最终效果处人工确认。

## 当前范围

包含：

- 共享离线桌宠运行内核。
- Petdex v1/v2 的本地目录、ZIP 和内部 slug 下载导入。
- 自动校验、标准化、预览和不可变来源记录。
- 配置驱动的单宠 Windows/macOS 构建。
- 客户素材制作流程的最后阶段验证。

不包含：

- 收款、订单、账户、DRM、激活码和在线商城。
- Windows 商业签名、macOS Developer ID、公证和正式商店发布。
- 自动更新、遥测、广告和运行时云服务。
- 未取得商业授权的社区宠物对外分发。

## 约束

- Wukong 交付目录和 Petdex 原始安装目录只读。
- 支持 v1 1536×1872 与 v2 1536×2288；版本识别同时参考声明和真实尺寸。
- 来源 slug、`pet.json.id` 和最终产品 ID 必须分开，避免覆盖与身份冲突。
- 导入和构建输出必须版本化，不覆盖历史结果。
- 真实透明窗口、拖动、缩放和动态动作验证是独立质量门。
- 客户素材缺失不阻塞 Phase 1–3。

## 架构方向

- `pet-package`：统一宠物包契约，保存来源、身份、图集、状态、版本和哈希。
- `pet-validator`：解析目录/ZIP，识别 v1/v2，检查路径、尺寸、alpha、JSON、帧和边界。
- `pet-importer`：只负责获取和复制到隔离工作区，不修改来源。
- `runtime-core`：窗口、渲染、交互、状态机、设置和日志，不包含角色文案与路径。
- `product-profile`：产品名、appId/bundleId、图标、默认宠物、版本和构建目标。
- `build-factory`：根据产品配置生成版本化候选安装包和清单。
- `customer-intake`：最后接入素材登记、角色基准、动作生成、视觉确认和标准包输出。

## 任务与依赖

### Phase 0：基线与计划

- [x] Task 0.1：盘点 Wukong 0.1.9 源码、发布证据和风险。
- [x] Task 0.2：安装并核验 v1 `doraemon` 与 v2 `dai`、`jokebear-codexpet` 测试样本。
- [x] Task 0.3：建立 PRD、项目规则、MEMORY 和任务清单。

### Checkpoint 0

- [ ] 用户确认 Phase 1 实施范围。

### Phase 1：共享运行内核

- [ ] Task 1.1：创建独立项目和最小可验证 Electron 基线。
- [ ] Task 1.2：定义统一宠物包与产品配置契约。
- [ ] Task 1.3：将 Wukong 渲染、动作和文案从硬编码改为数据驱动。
- [ ] Task 1.4：移植透明窗口、拖动、缩放、托盘、短动作和长待机。
- [ ] Task 1.5：加入通用状态回退，使 v1 没有注视行时仍可稳定运行。

### Checkpoint 1

- [ ] 自动测试和 lint 通过。
- [ ] Wukong source-mode 行为与参考基线一致。
- [ ] 替换测试宠物不需要改核心代码。
- [ ] 用户审查真实 macOS 演示和小尺寸截图/GIF。

### Phase 2：Petdex 导入与校验

- [ ] Task 2.1：实现本地目录导入和不可变来源快照。
- [ ] Task 2.2：实现安全 ZIP 导入，拒绝路径穿越、符号链接和异常文件。
- [ ] Task 2.3：实现 v1/v2 识别、JSON、尺寸、alpha、状态和哈希校验。
- [ ] Task 2.4：实现内部使用的 Petdex slug 下载适配器；运行器本身保持离线。
- [ ] Task 2.5：生成联系表、动作预览和标准化报告。
- [ ] Task 2.6：用 v1 `doraemon` 以及 v2 `dai`、`jokebear-codexpet` 完成端到端兼容验收。

### Checkpoint 2

- [ ] v1/v2 均可从原始输入导入并运行。
- [ ] 原始文件哈希保持不变。
- [ ] 无网络时已导入宠物仍可运行。
- [ ] 不完整或危险输入被明确拒绝。

### Phase 3：单宠安装包工厂

- [ ] Task 3.1：定义独立产品配置和冲突检查。
- [ ] Task 3.2：生成 Windows x64 source 与 win-unpacked 候选包。
- [ ] Task 3.3：生成 macOS Apple Silicon 候选应用。
- [ ] Task 3.4：生成版本化清单、文件大小、SHA-256 和交付说明。
- [ ] Task 3.5：在获得授权后完成真实 Windows 安装、卸载和 DPI 验收。

### Checkpoint 3

- [ ] 同一内核至少生成 Wukong、Doraemon和阿岱三个独立候选产品；JokeBear作为额外v2内部兼容样本。
- [ ] 三个产品的名称、身份、数据目录和资源互不冲突。
- [ ] 未签名与未验证边界标记清晰。

### Phase 4：客户素材制作

- [ ] Task 4.1：定义素材登记、授权状态和最低输入检查。
- [ ] Task 4.2：在取得测试素材后锁定角色身份基准并由用户确认。
- [ ] Task 4.3：制作并确认9种标准动作和16个注视方向。
- [ ] Task 4.4：运行图集、透明度、方向、小尺寸和动态视觉 QA。
- [ ] Task 4.5：输出标准宠物包并通过 Phase 3 生成独立候选安装包。

### Checkpoint 4

- [ ] 客户素材从输入到候选安装包形成完整、可追溯的单次工作流。
- [ ] 所有关键视觉确认和自动验证证据齐全。

## 主要风险

| 风险 | 影响 | 缓解 |
| --- | --- | --- |
| Wukong 代码高度硬编码 | 换宠物仍需改源码 | 先定义包契约和产品配置，再迁移行为 |
| v1 缺少 v2 注视方向 | 行为不一致 | 明确状态能力与回退策略，不伪造方向帧 |
| slug 与包内 ID 冲突 | 覆盖、数据混用 | 来源 slug、宠物 ID、产品 ID 分离并校验 |
| 旧 Electron 依赖公告 | 安全与维护风险 | 新项目实施前单独确认工具链并完整回归，不直接复制旧依赖 |
| macOS 不能证明 Windows | 错误发布结论 | 平台证据分开，真实 Windows 验收后再升级状态 |
| AI 生成动作身份漂移 | 客户返工 | 最后阶段执行，角色基准和逐阶段确认作为硬门 |

## 当前未决问题

- 通用项目的最终产品名称和品牌名尚未确定，当前使用“通用桌宠工作流”作为工作名。
- 新项目 Electron/toolchain 版本在 Phase 1 开始前根据当前官方版本、Wukong兼容性和安全风险确定。
- 客户素材测试图尚未提供，Phase 4 保持待定。
