# Petdex 命令导入计划

1. 先建立命令解析与固定 argv 启动边界，并用独立测试证明不会执行附加 Shell 内容。
2. 接入 main/preload IPC：下载后校验包、登记一次性候选令牌，确认时再复制到项目。
3. 在现有导入页加入命令输入、等待/错误/候选信息与确认交互。
4. 运行功能测试、全量测试、类型检查、构建和 Electron 界面检查。

主要风险是 GUI 环境找不到 `npx`、长时间下载和候选目录被篡改；分别通过常见路径解析、异步子进程超时、确认时重新校验与哈希比对处理。
