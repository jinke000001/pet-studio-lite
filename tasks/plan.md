# PRD 与实施计划：通用桌宠工作流

## 问题

Petdex Desktop 可以脱离 Codex 运行，但下载和社区依赖网络环境。当前 Wukong 成品证明独立 Electron 桌宠可行，却把角色、图集和产品配置写死。项目需要把这套经验转化为离线、配置驱动、可重复生产多个桌宠安装包的工作流。

## 成功标准

- 同一运行内核可加载 Wukong、v1 `doraemon` 以及 v2 `dai`、`jokebear-codexpet`，核心代码不因换宠物而修改。
- 安装后的宠物不依赖 Codex、Petdex、VPN、账户或运行时网络。
- 导入器能够识别 v1/v2、校验输入、保存来源与哈希，并拒绝结构不完整或不安全的包。
- 每只宠物可通过产品配置生成独立名称、图标、appId/bundleId、版本和数据目录。
- Windows 与 macOS 构建和验收结论分开记录；未签名产物只称测试版或候选版。
- 客户素材流程最终支持至少一张清晰、完整、无遮挡的全身正面图，允许 AI 辅助，并在角色基准、动作和最终效果处人工确认；只有目标产品实际使用 v2 注视能力时才制作方向帧。

## 当前范围

包含：

- 共享离线桌宠运行内核。
- Petdex v1/v2 的本地目录、ZIP 和内部 slug 下载导入。
- 自动校验、标准化、预览和不可变来源记录。
- 配置驱动的单宠 Windows/macOS 构建。
- 客户素材制作流程的最后阶段验证。

不包含：

- 收款、订单、账户、DRM、激活码和在线商城。
- Windows 商业签名、macOS Developer ID、公证和正式商店发布。
- 自动更新、遥测、广告和运行时云服务。
- 未取得商业授权的社区宠物对外分发。

## 约束

- Wukong 交付目录和 Petdex 原始安装目录只读。
- 支持 v1 1536×1872 与 v2 1536×2288；版本识别同时参考声明和真实尺寸。
- 来源 slug、`pet.json.id` 和最终产品 ID 必须分开，避免覆盖与身份冲突。
- 导入和构建输出必须版本化，不覆盖历史结果。
- 真实透明窗口、拖动、缩放和动态动作验证是独立质量门。
- 客户素材缺失不阻塞 Phase 1–3。

## 架构方向

- `pet-package`：统一宠物包契约，保存来源、身份、图集、状态、版本和哈希。
- `pet-validator`：解析目录/ZIP，识别 v1/v2，检查路径、尺寸、alpha、JSON、帧和边界。
- `pet-importer`：只负责获取和复制到隔离工作区，不修改来源。
- `runtime-core`：窗口、渲染、交互、状态机、设置和日志，不包含角色文案与路径。
- `product-profile`：产品名、appId/bundleId、图标、默认宠物、版本和构建目标。
- `build-factory`：根据产品配置生成版本化候选安装包和清单。
- `customer-intake`：最后接入素材登记、角色基准、动作生成、视觉确认和标准包输出。

## 任务与依赖

### Phase 0：基线与计划

- [x] Task 0.1：盘点 Wukong 0.1.9 源码、发布证据和风险。
- [x] Task 0.2：安装并核验 v1 `doraemon` 与 v2 `dai`、`jokebear-codexpet` 测试样本。
- [x] Task 0.3：建立 PRD、项目规则、MEMORY 和任务清单。

### Checkpoint 0

- [x] 用户确认 Phase 1 实施范围。

### Phase 1：共享运行内核

- [x] Task 1.1：创建独立项目和最小可验证 Electron 基线。
- [x] Task 1.2：定义统一宠物包与产品配置契约。
- [x] Task 1.3：将 Wukong 渲染、动作和文案从硬编码改为数据驱动。
- [x] Task 1.4：移植透明窗口、拖动、缩放、托盘、短动作和长待机。
- [x] Task 1.5：加入通用状态回退，使 v1 没有注视行时仍可稳定运行。

### Checkpoint 1

- [x] 自动测试和 lint 通过。
- [x] Wukong source-mode 行为与参考基线一致。
- [x] 替换测试宠物不需要改核心代码。
- [x] 用户审查真实 macOS 演示和小尺寸截图/GIF。

### Phase 2：Petdex 导入与校验

- [x] Task 2.1：定义导入契约、错误代码、版本化目录和安全限额。
  - 验收：来源类型、来源身份、文件清单、SHA-256、授权状态和标准包路径都有明确字段。
  - 验证：契约单元测试覆盖合法输入、越界路径、非法 slug 和限额。
- [x] Task 2.2：实现本地目录导入和不可变来源快照。
  - 验收：只读遍历来源；拒绝符号链接和异常文件；以临时目录写入后原子改名；不覆盖既有导入。
  - 验证：集成测试确认来源哈希不变、重复导入可追溯且失败不留半成品。
- [x] Task 2.3：实现安全 ZIP 导入。
  - 验收：拒绝绝对路径、路径穿越、符号链接、重复路径、加密条目、超限文件和压缩炸弹风险。
  - 验证：针对每类危险 ZIP 的拒绝测试，以及合法 v1/v2 ZIP 导入测试。
- [x] Task 2.4：实现 v1/v2 识别、JSON、尺寸、alpha、状态和哈希校验。
  - 验收：声明版本与真实尺寸交叉校验；标准化输出可直接由 Phase 1 运行器加载。
  - 验证：v1、v2、版本冲突、损坏 WebP、缺少透明度和缺少文件测试。
- [x] Task 2.5：生成联系表、动作预览和标准化报告。
  - 验收：输出静态联系表、关键动作预览和机器可读/人可读报告，不修改标准图集。
  - 验证：尺寸、帧数、文件哈希和报告字段检查。
- [x] Task 2.6：实现内部 Petdex slug 下载适配器；运行器本身保持离线。
  - 验收：slug 严格校验；命令参数不经过 shell；下载进入隔离临时目录后再走同一导入管线。
  - 验证：使用受控假 CLI 测试参数、失败清理和来源识别。
- [x] Task 2.7：用 v1 `doraemon` 以及 v2 `dai`、`jokebear-codexpet` 完成端到端兼容验收。
  - 验收：三个样本从原始目录导入后断网可运行；原始哈希不变；报告完整。
  - 验证：完整测试、lint、npm audit、真实透明窗口与小尺寸预览检查。

### Phase 2 威胁模型与安全边界

- 不可信输入：本地目录树、ZIP 条目和元数据、`pet.json`、图集字节、Petdex slug 与 CLI 输出。
- 受保护资产：项目外文件、原始宠物目录、历史导入结果、标准宠物包身份与哈希证据。
- 主要风险：路径穿越、符号链接逃逸、覆盖、资源耗尽、命令注入、身份冲突和不完整输出被误当成成功导入。
- 控制：允许列表、真实路径边界检查、文件数/单文件/总大小限额、无 shell 子进程、临时目录与原子改名、版本化非覆盖输出、明确错误代码。

### Checkpoint 2

- [x] v1/v2 均可从原始输入导入并运行。
- [x] 原始文件哈希保持不变。
- [x] 无网络时已导入宠物仍可运行。
- [x] 不完整或危险输入被明确拒绝。

### Phase 3：单宠安装包工厂

- [x] Task 3.1：定义独立产品配置、应用身份和冲突检查。
  - 验收：每个产品明确版本、appId、可执行文件名、产品名、数据目录身份、目标架构和图标策略；跨产品关键身份不得重复。
  - 验证：合法配置、缺失字段、危险标识、重复身份和路径逃逸测试。
- [x] Task 3.2：实现版本化、非覆盖的单宠资源装配与构建工厂。
  - 验收：只装配一个产品和一个标准宠物包；历史构建目录不得覆盖；构建时不读取用户 Petdex/Codex 宠物目录。
  - 验证：临时目录集成测试核对文件白名单、产品选择和宠物图集哈希。
- [x] Task 3.3：生成 Windows x64 win-unpacked 与未签名 NSIS 候选包。
  - 验收：产品名称、appId、EXE 名、快捷方式策略和卸载身份独立；关闭发布与签名发现；产物明确标为内部候选。
  - 验证：构建命令成功，解包资源与目标配置、宠物哈希一致；Mac 侧只作静态候选验证。
- [x] Task 3.4：生成 macOS Apple Silicon 未签名 `.app` 候选应用。
  - 验收：bundleId、应用名和用户数据身份独立；不生成 DMG、不签名、不公证。
  - 验证：真实启动 `.app`，日志出现对应产品的 `runtime-ready` 与 `renderer-ready`。
- [x] Task 3.5：完成三主样本和一个额外样本的身份隔离与兼容验证。
  - 验收：Wukong、Doraemon、阿岱为主样本，JokeBear 为额外 v2 内部兼容样本；四者构建身份互不冲突。
  - 验证：配置矩阵、构建清单和实际打包资源逐项比对。
- [x] Task 3.6：生成版本化清单、文件大小、SHA-256 和交付说明。
  - 验收：机器可读与人可读清单记录工具版本、命令、平台、文件、哈希、授权和未验证边界。
  - 验证：重新计算所有清单文件哈希并核对，无历史目录覆盖。
- [x] Task 3.7：使用已授权的独立交付完成真实 Windows 安装、卸载和 DPI 验收。
  - 验收：source、win-unpacked、installed mode 分开；覆盖 100%/125%/150% 冷启动、交互、生命周期和恢复系统设置。
  - 验证：返回版本化 Windows 报告、截图、环境数据、卸载/重装结果和精确安装包 SHA-256。
  - 最终状态：2026-08-31 Kimi 已从提交 `a60f3b7` 的受控源码在 Windows 本地 NTFS 原生、非覆盖重建四套候选；四产品 source、win-unpacked、installed mode、100%/125%/150% 冷启动、真实动态 DPI、核心交互、官方卸载、同包重装和身份隔离全部通过。T7 回传 1352/1352 哈希已在 Mac 侧独立复核，Task 3.7 关闭。

### Checkpoint 3

- [x] 同一内核至少生成 Wukong、Doraemon 和阿岱三个独立候选产品；JokeBear 作为额外 v2 内部兼容样本。
- [x] 四个产品的名称、身份、数据目录和资源互不冲突。
- [x] 未签名与未验证边界标记清晰。
- [x] 四产品 Windows source、win-unpacked、installed mode、卸载/重装和 DPI 验收完成；最终状态仍为未签名内部候选。

### Phase 4：客户素材制作

- [x] Task 4.1：登记五张原始素材、尺寸、哈希、授权状态和使用角色。
- [x] Task 4.2：锁定无服装、无道具的正面全身图为唯一身份基准并由用户确认。
- [x] Task 4.3：准备版本化 hatch-pet 制作运行目录与视觉任务图。（最初按 v2 建立，最终输出按后续 v1 决策收尾）
  - 验收：原始素材只读；身份、风格、禁用元素、chroma key、生成依赖与输出路径均写入运行清单。
  - 验证：运行目录非覆盖，所有参考文件哈希与登记清单一致。
- [x] Task 4.4：生成并确认角色主视觉基准。（Codex 候选已确认；Kimi 独立候选需重新过门）
  - 验收：同一橙色角色、奶白脸区、实体眼睛、短肢圆身；无服装、道具、文字、场景和阴影。
  - 验证：用户在正常桌宠尺寸确认后才进入动作生成。
- [x] Task 4.5：制作并验证 9 种标准动作。
  - 验收：每行身份一致、语义正确、帧数正确、姿势分离、无裁切/漂浮效果/阴影/尺寸跳变。
  - 验证：逐行动作检查、8×9 联系表和 GIF 预览；用户确认后锁定九行动作。
- [x] Task 4.6：确认注视方向是否属于目标功能范围。
  - 决定：2026-08-28 用户确认只与 Petdex 网站宠物核心功能对齐；小福猩按 v1 收尾，不制作四个基准方向和 16 个注视方向。
  - 边界：运行器继续兼容 v2 导入宠物，不删除或弱化已有 v2 能力。
- [x] Task 4.7：组装、清理并验证 v1 标准包。
  - 验收：`1536×1872`、8×9、`pet.json` 不声明 v2、alpha 与 chroma 检查通过；批准图集、原始素材和历史候选不被覆盖。
  - 验证：来源与标准包哈希一致、九动作联系表/GIF、自动验证 JSON 和独立最终视觉 QA。
- [x] Task 4.8：接入通用运行器并通过 Phase 3 生成独立候选。
  - 验收：项目本地标准包、产品配置、source-mode、macOS `.app`、Windows x64 `win-unpacked` 与未签名 NSIS 候选齐全。
  - 验证：Mac 真实窗口、包内单宠 ASAR、图集哈希和候选清单通过。
- [x] Task 4.9：Phase 4 完成后执行 Windows 正式验收。
  - 验收：source、win-unpacked、installed mode、卸载/重装和 100%/125%/150% DPI 分开验证。
  - 验证：使用返回的精确候选和 SHA-256，不以 macOS 结果代替。
  - 最终状态：0.1.5 已通过 Windows source、win-unpacked、installed mode、100%/125%/150% 冷启动与动态 DPI、核心交互、三次同版本覆盖、官方卸载和同包重装；514/514 回传哈希已在 Mac 侧复核，系统缩放恢复 100%。

### Checkpoint 4

- [x] 客户素材从输入到候选安装包形成完整、可追溯的单次工作流。
- [x] 候选范围内的关键视觉确认、自动验证和 Windows 正式验收证据齐全；0.1.5 保持未签名内部候选，不升级为正式发布版。

### Phase 5：项目收尾与内部基线

- [x] Task 5.1：确认收尾 PRD，冻结范围为状态统一、最终证据索引、轻量归档策略、回归验证和可选 Git 基线。
- [x] Task 5.2：统一 `README.md`、本计划、任务清单和 MEMORY 的当前状态；历史阶段证据保留当时语境。
- [x] Task 5.3：新增最终项目收尾报告，集中记录能力、边界、精确报告路径、关键 SHA-256、授权状态和已知网络观察项。
- [x] Task 5.4：采用“T7 完整证据＋本机轻量索引”；不把约 3.9 GiB Task 3.7 RETURN 复制到当前仅约 16 GiB 可用空间的 Mac，本机只保存可复核路径和哈希。
- [x] Task 5.5：用户确认 Checkpoint 5A 后完成回归；`npm test` 77/77、lint、source preflight 和 `npm audit --omit=dev` 全部通过。audit 前两次受 Registry TLS 重置阻断，第三次未改配置重试成功并返回 0 vulnerabilities。
- [x] Task 5.6：最终 diff 五轴审查无阻断项；用户授权只创建一个内部基线提交，不创建标签、不合并分支、不推送远端。

### Checkpoint 5A：文档与证据收尾

- [x] 当前状态文档不再把 Task 3.7 写成待办。
- [x] T7 原报告、候选、RETURN 与历史证据均未修改或覆盖。
- [x] 收尾报告可以独立定位最终证据，并明确“内部候选，不是正式发布版”。
- [x] 用户已审查并确认进入回归验证；四项回归全部通过，并已单独授权只创建一个内部基线提交。

### Phase 6：桌宠制作台 MVP

用户于 2026-09-01 确认 Phase 6 PRD，并创建目标模式。完整问题、范围、约束、架构与 Phase 6.1–6.6 计划见 `tasks/phase-6-workbench-prd.md`。

- [x] Phase 6.1：工作台契约、独立安全壳层、非覆盖项目存储、关闭后恢复和 Kimi 交接包。
- [ ] Phase 6.2：目录/ZIP 导入、校验、动作与真实桌宠预览闭环。
- [ ] Phase 6.3：产品配置、标准包/项目包和双平台候选导出。
- [ ] Phase 6.4：后台任务、取消、失败证据、安全重试和历史恢复。
- [ ] Phase 6.5：首次引导与 Kimi 视觉收口，Codex 负责真实数据接入和工程审查。
- [ ] Phase 6.6：自动回归、macOS 全流程候选和独立 Windows 验收准备。

### Checkpoint 6.1：工作台基础

- [x] 工作台能独立启动、创建制作项目并在关闭重开后恢复。
- [x] renderer 保持 sandbox、context isolation、无 Node 权限，IPC 输入在主进程边界校验。
- [x] 项目目录版本化且不覆盖，路径输入不能逃逸工作区。
- [x] 现有五个桌宠入口与回归不受影响。
- [x] Kimi 获得固定页面清单、字段规则、状态矩阵、错误示例和模拟数据。

2026-09-01 用户已统一授权自主完成 Phase 6.2–6.6，不再逐阶段等待确认。Phase 6.2 的已确认最小 PRD 已落盘到 `tasks/phase-6.2-workbench-prd.md`；每阶段仍须通过自动验证、真实运行、审查、证据同步和聚焦本地 checkpoint，不能以静态检查替代后续门禁。

## 主要风险

| 风险 | 影响 | 缓解 |
| --- | --- | --- |
| Wukong 代码高度硬编码 | 换宠物仍需改源码 | 先定义包契约和产品配置，再迁移行为 |
| v1 缺少 v2 注视方向 | 非目标功能被误当成缺陷 | 按能力回退到 idle；只有产品明确需要注视交互时才制作 v2 方向帧 |
| slug 与包内 ID 冲突 | 覆盖、数据混用 | 来源 slug、宠物 ID、产品 ID 分离并校验 |
| 旧 Electron 依赖公告 | 安全与维护风险 | 新项目实施前单独确认工具链并完整回归，不直接复制旧依赖 |
| macOS 不能证明 Windows | 错误发布结论 | 平台证据分开；小福猩与四产品均已用真实 Windows 报告关闭对应验收门，今后新候选继续遵守同一规则 |
| AI 生成动作身份漂移 | 客户返工 | 最后阶段执行，角色基准和逐阶段确认作为硬门 |
| T7 是 Task 3.7 完整证据主副本 | 单盘损坏会降低可追溯性 | 当前先保留完整 T7 原件和本机轻量哈希索引；如需更高容灾，后续复制到另一块外置盘，不挤占当前 Mac 空间 |
| Windows 构建机直连 GitHub 超时 | 后续原生构建可能再次受阻 | 当前作为非阻塞观察项保留；仅在再次复现时核查，不预先修改永久镜像、代理或系统网络设置 |

## 当前未决问题

- 通用项目的最终产品名称和品牌名尚未确定，当前使用“通用桌宠工作流”作为工作名。
- Phase 3 沿用 Electron 43.4.1、electron-builder 26.15.3 和 Node.js 22.12+；首轮使用通用测试图标，正式角色图标以后单独确认。
- Phase 4 小福猩 Petdex v1 链路、0.1.5 Windows 安装生命周期验收和 Phase 3 Task 3.7 四产品 Windows 验收均已收口；Kimi Code 独立候选保持隔离。当前只进行 Phase 5 项目收尾，商业发布体系继续暂缓。
- Windows 构建机直连 GitHub 的 `ETIMEDOUT 20.205.243.166:443` 暂时保留观察，不影响已通过的 Task 3.7 结论。
