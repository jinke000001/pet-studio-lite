import React from 'react';
import ReactDOM from 'react-dom/client';
import { SizeControlApp } from './SizeControlApp';
import './size-control.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <SizeControlApp />
  </React.StrictMode>,
);
