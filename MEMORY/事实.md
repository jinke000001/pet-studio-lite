# 事实

## Wukong 基线

- 参考版本：0.1.9，Electron 31.7.7，electron-builder 24.13.3。
- 源码：`Wukong-Windows-Desktop-Pet-Handoff-20260731/Windows-Release-0.1.9-20260801/Source/`。
- 2026-08-27 只读复核：`npm test` 15/15 通过，`npm run lint` 通过。
- Windows 历史验收报告记录 source、win-unpacked、installed mode 通过，200Hz 显示器目标拖动间隔 5ms；125%/150% DPI 未验证。
- 安装包 SHA-256：`d49e6e6ccd5c82bbfa94f37413fb7b5b9bcd997bdce7c7f0ed50c933b269ea33`。
- win-unpacked EXE SHA-256：`8ab2a5fda48b1ae400407a08661a4156d2a0efd267530da6db51940ae88386fd`。
- app.asar SHA-256：`c57b150e13482a8d98b52b82cd4eddbf239da2dcf4e7ce03c3748473cd759639`。
- 批准 WebP SHA-256：`349cb1afe921058f803f51fd05e658b79d97f72fb901134fc2535091d2f3cd05`。
- Wukong 源码快照不是 Git 仓库，且不包含 `node_modules`。

## Petdex 测试样本

- 历史安装 `~/.codex/pets/lulu-capybara-2/` 是 1536×1872 的 v1，但用户说明这是以前使用过的宠物，因此不再作为当前 v1 测试样本；文件保留且未覆盖。
- v2：2026-08-27 使用 `npx -y petdex@latest install dai` 安装成功。
- `dai` 声明 `spriteVersionNumber: 2`，图集为 1536×2288 WebP 且有 alpha。
- `dai` 图集 SHA-256：`48865e93e681d089e9151b65e388cd23f7549b02248b76aa1fc2334f80781c7f`。
- 2026-08-27 使用 `npx -y petdex@latest install jokebear-codexpet` 安装成功。
- `jokebear-codexpet` 实际声明 `spriteVersionNumber: 2`，图集为 1536×2288 WebP 且有 alpha，不能替代 v1 样本。
- `jokebear-codexpet` 图集 SHA-256：`86662b0ad39947c5ba2b732d7626e4bbda89ecacc68d484eae75053d1f6c44c9`；Codex 与 Petdex Desktop 两份图集哈希一致。
- `jokebear-codexpet` 的描述明确标注其为基于第三方 IP 的非官方改编，只用于内部技术兼容测试。
- 2026-08-27 查询 Petdex 当前清单后确认 `doraemon` 标记为 `spriteVersionNumber: 1`，随后使用 `npx -y petdex@latest install doraemon` 安装成功。
- `doraemon` 的实际 `pet.json` 未声明 `spriteVersionNumber`，图集为 1536×1872 WebP 且有 alpha，确认作为 v1 测试样本。
- `doraemon` 图集 SHA-256：`1af435fc385dc935522edf13cefbe994c7c078ced46463b66ad6e401f72464c8`；Codex 与 Petdex Desktop 两份图集哈希一致。
- `doraemon` 是第三方 IP 相关素材，只用于内部技术兼容测试。
- 所有 Petdex 样本只用于内部技术兼容测试，当前不构成商业分发授权。

## 当前缺口

- 尚无客户素材制作测试图；该流程放在 Phase 4，不阻塞 Phase 1–3。
- 已建立 Git `main` 基线和 `feature/shared-runtime` 分支；Phase 1 运行器代码截至提交 `ef0d577`。
- 共享内核自动测试 20/20、lint 和 `npm audit --omit=optional` 已通过。
- Wukong、v1 Doraemon、v2 Dai 和 v2 JokeBear 已在 macOS 用同一核心真实启动；四张 Retina 窗口截图均为 288×312 且含 alpha。
- Wukong 单击已通过真实界面触发并显示配置文案；界面自动化不能可靠抓取透明窗口做坐标拖动，因此该部分由用户手动验收覆盖。
- 2026-08-27 用户完成 Wukong 真实效果测试并反馈“效果比较满意”，Phase 1 用户验收通过。
- 尚未对当前通用内核执行 Windows 验收，也未生成安装包。
- 2026-08-27 用户确认开始 Phase 2；实施分支为 `feature/pet-importer`，Phase 3 安装包与 Windows 验收仍不在本阶段范围。
- 2026-08-27 用户确认 Phase 3 PRD 与默认构建方案；实施分支为 `feature/package-factory`。

## Phase 2 完成事实

- 已实现目录、ZIP、Petdex slug 三种入口，以及非覆盖版本化输出、来源快照、标准包、JSON/Markdown 报告、SVG 联系表和离线动作预览。
- 自动测试 47/47、lint 和 `npm audit --omit=optional` 通过。
- Doraemon v1、Dai v2、JokeBear v2 已从 `~/.codex/pets/` 原始目录导入；原始文件、来源快照和标准包 WebP 哈希核对一致。
- 三份导入标准包已由 Electron source-mode 真实启动，日志均有 `runtime-ready` 与 `renderer-ready`。
- 三张真实联系表布局检查通过；三份动作预览在 Chromium 中为 0 errors、0 warnings，且 Doraemon 动画帧位置随时间推进。
- 真实 Petdex `doraemon` slug 隔离下载成功，下载哈希与现有本地原始样本一致；没有写入或覆盖用户宠物目录。
- 本机最终证据位于被 Git 忽略的 `imports/phase-2-evidence-v2/` 和 `imports/phase-2-live-slug-v2/`；可复核摘要位于 `Resources/phase-2-import-evidence.md`。
- Phase 2 Checkpoint 2 已通过；尚未生成安装包，也没有当前通用内核的 Windows 验收结论。

## Phase 3 候选构建事实

- 配置驱动的单宠构建工厂已经完成；Wukong、Doraemon、阿岱和 JokeBear 均生成 Windows x64 `win-unpacked`、未签名 NSIS 与 macOS Apple Silicon 未签名 `.app`。
- 四个产品 `productId`、appId/bundle ID、EXE 名和用户数据身份互不冲突；每个 `app.asar` 只包含选定产品配置与选定宠物包。
- 同产品 macOS/Windows `app.asar` 字节一致，包内图集哈希与来源图集一致；四个 macOS `.app` 真实启动均出现 packaged `renderer-ready` 和 `runtime-ready`。
- 自动测试 64/64、lint、npm audit 和 20 个最终关键产物重新哈希通过；证据摘要位于 `Resources/phase-3-package-evidence.md`，本机完整候选位于被 Git 忽略的 `release/candidates/`。
- Checkpoint 3 候选构建与 Mac 侧验证已通过；Windows source、win-unpacked、installed mode、卸载/重装和 100%/125%/150% DPI 正式验收仍未执行。

## Phase 4 启动事实

- 2026-08-27 用户确认 Phase 4 PRD；实施分支为 `feature/xiaofuxing-v2`。
- 用户将五张参考 JPG 放入 `小福猩素材/`；四张为 1064×2304，一张为 1920×1080，文件哈希登记于 `Resources/xiaofuxing-source-manifest.md`。
- 用户确认无服装、无道具的正面完整全身图 `307322fa5ca9ee062be71a318055a043.jpg` 为唯一身份基准；其他四张只作姿势、表情和侧面结构参考。
- 当前授权状态保守记录为 `internal-test`；Phase 4 使用项目本地版本化输出，不写入用户 Codex/Petdex 宠物目录。
- 因暂时没有 Windows 环境，Windows 正式验收在 Phase 4 完成后执行。
- 项目本地运行目录 `runs/phase-4-xiaofuxing-v2-20260827-01/` 已建立；主视觉候选按 `#FF00FF`、阈值 96 提取后的 192×208 预览轮廓完整，并于 2026-08-27 获用户确认，后续动作行以 `references/canonical-base.png` 为身份基准。
- 九种标准动作候选已完成：`qa/review.json` 无错误，待机行采用经 GIF 确认稳定的 `stable-slots`，其余八行使用组件提取；隔离视觉审查通过。首次镜像左跑因碎片被拒绝，已由独立生成左跑行替换。当前等待用户视觉确认。
- 2026-08-28 用户反馈 Codex 标准动作整体效果一般，决定让 Kimi Code 制作独立 Phase 4 候选。Kimi 交接位于 `Resources/kimi-phase-4-handoff.md`，项目 Skill 位于 `.kimi-code/skills/xiaofuxing-phase4/SKILL.md`；现有 Codex 候选保留且未进入方向制作。
- 2026-08-28 用户确认 Codex 九动作修正版 PRD；新目录为 `runs/phase-4-xiaofuxing-v2-codex-repair-20260828-01/`，原始素材、旧 Codex 候选和 Kimi 候选均未覆盖。
- 修正版重做 `idle`、`running-right`、`running-left`、`failed`、`waiting`；`waving`、`jumping`、`running`、`review` 从旧候选按字节保留。左跑独立生成，不镜像右跑。
- 五个重做行逐行检查通过；九行动作全量 `qa/review.json` 为 `ok: true`、零错误、零警告，隔离视觉复核通过。1536×1872 WebP SHA-256 为 `8af2e03dea795f653dd64b155cb0abd690f8093d765e6127a4a2cb3c0a411029`；证据位于 `Resources/xiaofuxing-standard-actions-repair-evidence.md`。当前等待用户视觉确认，不进入方向制作。
- 2026-08-28 用户确认修正版中的 `idle`、`waving`、`jumping`、`failed`、`waiting`、`running`、`review` 可以保留；`running-right` 与 `running-left` 仍不满意。用户将提供跑动素材参考；下一轮只修两行，七个已确认行保持不变。
- 2026-08-28 用户新增 `IMG_3538.GIF`、`IMG_3539.GIF`、`IMG_3540.GIF` 三段跑步参考，明确偏好三分之四正面角度、可爱短步和清晰轮廓。新建非覆盖目录 `runs/phase-4-xiaofuxing-v2-codex-running-ref-20260828-01/`；三个源 GIF 以原始字节和 SHA-256 登记，七个已确认动作行经 `cmp` 复核保持逐字节一致。
- 参考驱动的 `running-right` 新候选已完成：8/8 帧、组件提取与结构检查 `ok: true`、零错误、零警告；独立小尺寸视觉复核确认方向、三分之四正面、身份、短步弹跳与清晰轮廓通过。用户于 2026-08-28 回复“我觉得还可以，确认”，右跑用户确认门关闭；当前按相同标准独立制作 `running-left`，禁止镜像。
- 参考驱动的 `running-left` 已独立生成，未镜像或复用右跑帧：8/8 帧逐行检查通过；九动作全量 `qa/review.json` 为 `ok: true`、零错误、零警告。独立小尺寸视觉复核确认朝屏幕左方、三分之四正面、双眼和奶油脸可读，体型与材质和已确认右跑一致，无叠影、堆叠、拖影或反向步态。当前等待用户确认左跑。
- 全量检查曾因 `frames/running-right/` 与 `frames/running-left/` 各混入一份源条带 `selected-output.png` 而误计为 9 帧；已将两份源条带移至 `frames/source-strips/`，动作目录恢复严格 8 帧，随后全量复核通过。该处理未重做或修改生成图像。
- 2026-08-28 用户指出独立左跑腿部动作不流畅，要求保留该版本并采用已确认右跑的镜像。独立左跑条带、8 帧、GIF 和联系表已保存到 `baseline/` 并记录哈希。
- 首次对原始生成条带按固定槽镜像会切断跨槽手臂，失败证据已保留；活动左跑改为对八张已通过检查的 192×208 右跑帧分别水平镜像，帧顺序不变。逐帧像素比较确认精确镜像；左跑逐行与九动作全量检查均为零错误、零警告；独立小尺寸视觉复核确认腿部节奏连续、无反向时序、无肢体缺损，其他七行无退化。当前等待用户确认镜像左跑。
- 2026-08-28 用户回复“确认”，镜像 `running-left` 获用户确认，九种标准动作确认门全部关闭。批准的 1536×1872 WebP SHA-256 为 `3a7e32bf72e9d109eb2f955ebd748ee4da21064c98a2c07f2956c9f1652a049a`；联系表 SHA-256 为 `9c38f958f3932156b572a9e98f692a45665fb394644dc903080c6988d7656912`；`qa/review.json` SHA-256 为 `47e7c138b9f934185bc370b9018b1bf12a3de9229e3dfa063feb3dd9f322fb96`，零错误、零警告。批准清单位于运行目录的 `qa/standard-actions-approval.json`。
- 2026-08-28 用户确认“按 v1 收尾”：小福猩最终使用 8×9、1536×1872 标准包，不制作四基准方向和 16 注视方向；历史 v2 运行目录保持只读证据性质，共享运行器的 v2 兼容不变。该决定尚不等于候选构建或 Windows 发布通过。
- 小福猩 v1 最终图集完成一次确定性 chroma 去色溢：alpha 保持不变，`validate_atlas.py` 识别为 8×9 v1、RGBA、零错误、零警告、透明 RGB 残留为 0；最终 WebP SHA-256 为 `427a7a681110cb77d01e7878191052a9f6dd4973062577fe34b8759c0b523660`。
- `config/products/xiaofuxing.json` 和 `local-pets/xiaofuxing/` 已接入共享运行器；64/64 测试及 lint 通过。source-mode 真实窗口日志包含 `renderer-ready`、`runtime-ready`，两张 Retina 捕获间有 42,500 个像素变化，证明待机动画推进。
- 双平台候选位于 `release/candidates/xiaofuxing-desktop-pet/0.1.0/candidate-20260828144716973/`。macOS `.app` 真实启动通过；macOS/Windows ASAR 均只含小福猩配置与包，包内图集哈希与来源一致。未签名 NSIS SHA-256 为 `ab56ab9d340ba7eee189c39312706138d5c5f47b31a486d9ad3c9dae8c4b0157`。
- Phase 4 从客户素材到内部候选的链路已闭合。Windows source、win-unpacked 真实运行、installed mode、卸载/重装和 100%/125%/150% DPI 仍未验证；当前候选不是正式发布版。
- 2026-08-28 已创建非覆盖 T7 Windows 验收交付：`/Volumes/T7 Shield/小福猩桌宠/xiaofuxing-v1-windows-acceptance-20260828-01/`，总计约 469 MiB、96 个文件（其中 `checksums.sha256` 校验 95 个输入文件）。交付包含 source ZIP、完整 `win-unpacked`、精确未签名安装包、PRD、Windows Codex 提示词、报告模板、PowerShell 校验脚本、精选证据和空 `RETURN/`。
- source ZIP 为 1,590,297 字节，SHA-256 `a7bdfd82fecb132ab19e4f829ee4b931f09ffda1b7476dde61143f0f98fa7b26`；共 71 个条目，3 个非 ASCII 路径均有 UTF-8 标记，extra fields、`._*`、`.DS_Store` 均为 0。隔离解压后 `npm ci`、64/64 测试、lint 和 `npm audit` 0 vulnerabilities 通过。
- T7 交付中的安装包、`win-unpacked` EXE、`app.asar` 和候选清单 SHA-256 分别为 `ab56ab9d340ba7eee189c39312706138d5c5f47b31a486d9ad3c9dae8c4b0157`、`994374ee5f92803b3fc56e5bbfdb7d2b82bc29b1e0c28e36646d2bf4a719e872`、`6348833dec8932f2c3657f2b2e57837882cadb10cf3ef6ceeca433ca0caf2d71`、`cf901c148c6a3823707e0b34d0f73b4a9a922605c38ff0f3486b3d72806b999a`，与原候选一致；最终递归元数据检查为 0。
