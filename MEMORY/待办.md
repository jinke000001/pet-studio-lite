# 待办

## Phase 0

- [x] 找到 Wukong 0.1.9 最终源码快照、安装包和 Windows 验收报告。
- [x] 只读复核 Wukong 0.1.9 测试、lint 和关键哈希。
- [x] 安装并确认 v2 样本 `dai` 的版本、尺寸和哈希。
- [x] 安装并核验 `jokebear-codexpet`；确认它实际为 v2，不能替代 v1 样本。
- [x] 查询、安装并核验 v1 样本 `doraemon` 的版本、尺寸和哈希。
- [x] 写入正式 PRD、实施计划、项目规则和 MEMORY。
- [x] 用户确认 Phase 1 的实施范围并创建新的通用运行内核代码基线。

## 后续阶段

- [x] Phase 1：用户已完成真实效果测试并表示比较满意，Checkpoint 1 关闭。
- [x] Phase 2：已实现 Petdex v1/v2 目录/ZIP/slug 导入、校验、预览和标准化，Checkpoint 2 通过。
- [x] Phase 3：配置驱动的单宠独立安装包工厂、双平台候选和 Mac 侧验证完成；Windows 正式验收仍单独保留。
- [x] Phase 4 候选链路：小福猩已按 Petdex v1 完成 8×9 标准包、运行器接入、双平台未签名候选和 Mac 侧真实验证；Kimi 独立候选保持隔离。
- [x] 小福猩 Windows 验收交付已整理到 T7 新目录，source ZIP、`win-unpacked`、安装包、PRD、提示词、报告模板、证据与全量哈希均已验证。
- [x] 检查 0.1.0 Windows 回传并定位 source 路径兼容、Electron 缺失预检、150%/动态 DPI 与官方卸载失败。
- [x] 完成 0.1.1 针对性修复、显式 Electron 安装、75/75 回归（连续 5 轮通过）、批准素材哈希保护、macOS 真实运行及双平台候选静态验证。
- [x] 检查首轮 0.1.1 T7 回传，定位 Windows ASAR 嵌套路径提取失败，并完成 `634ed4e` 修复、76/76 回归和新双平台候选验证。
- [x] 经用户确认后，将修复后的非覆盖 0.1.1 第二轮复验包写入 T7 `-04` 目录，并完成 90/90 哈希、ZIP UTF-8、隔离解压和零元数据校验。
- [x] 检查 0.1.1 第二轮 Windows 回传，定位 source 动态 150% 白窗/裁切与临时边界脉冲，并完成 0.1.2 修复、回归、Mac 真实窗口及双平台候选验证。
- [x] 将 0.1.2 非覆盖复验包写入 T7，并完成 93/93 哈希、ZIP UTF-8、隔离解压和零元数据校验。
- [x] 只读检查 0.1.2 Windows 回传，确认受控重试误启 Wukong 且 DPI、截图、产品日志和进程证据不合格；建立非覆盖 `-02-docfix` 修正版提示词、审查日志和报告模板。
- [x] 正确小福猩 source 与 win-unpacked 已完成 100%/125%/150% 冷启动和真实动态 DPI；安装冲突审计确认 NSIS 检测误判，完成 0.1.3 精确主程序路径检测、77/77、双平台候选和 Mac packaged runtime 验证。
- [x] 将 0.1.3 非覆盖安装生命周期复验包写入 T7，完成 source ZIP UTF-8/隔离解压、108/108 输入哈希和递归零元数据校验。
- [x] 0.1.5 已完成 Windows installed mode、100%/125%/150% 冷启动与动态 DPI、三次同版本覆盖、官方卸载和同包重装；514/514 回传哈希通过，Phase 4 Checkpoint 4 关闭。
- [x] 经用户授权，将 Phase 3 Task 3.7 的受控源码、历史候选审计、PRD、Windows 提示词、报告模板和工具写入 T7 新目录；36/36 输入哈希、隔离源码 77/77 与递归零元数据检查通过。
- [x] Kimi 已在 Windows 原生构建 Wukong、Doraemon、阿岱和 JokeBear 四套新候选，并完成 source、win-unpacked、installed mode、卸载、同包重装、100%/125%/150% 冷启动与真实动态 DPI 验收；T7 回传 1352/1352 哈希复核通过，Task 3.7 关闭。
- [ ] 保留观察 Windows 构建机直连 GitHub 的 `ETIMEDOUT 20.205.243.166:443`；本轮已用单次命令级 `ELECTRON_MIRROR` 重试完成，不主动修改永久 npm 配置、系统代理或网络设置，仅在后续原生构建再次复现时核查。
- [x] 用户确认 Phase 5 收尾 PRD；范围限于状态统一、最终证据索引和轻量归档策略，不进入商业发布或新功能。
- [x] 完成 Phase 5 当前状态文档统一、最终收尾报告和 T7 轻量证据索引，提交用户审查 Checkpoint 5A。
- [x] 用户已确认 Checkpoint 5A；`npm test` 77/77、lint 和 source preflight 通过。
- [x] `npm audit --omit=dev` 第三次未改配置重试成功，返回 0 vulnerabilities；前两次 Registry TLS 连接重置保留为过程证据。Phase 5 四项回归全部通过。
- [x] 最终 diff 五轴审查无阻断项；用户授权只创建一个内部基线提交，不创建标签、不合并分支、不推送远端。

商业发布体系已明确暂缓，不列入当前执行待办。

## Phase 6：桌宠制作台 MVP

- [x] 用户确认 Phase 6 PRD、Codex/Kimi 分工与目标模式。
- [x] Phase 6.1：已完成契约、安全壳层、非覆盖项目存储、关闭后恢复、Kimi 交接包和验证。
- [x] Checkpoint 6.1：用户审查可运行的工作台基础后，再进入 Phase 6.2 导入/校验/预览闭环。
- [x] Phase 6.2：目录/ZIP 导入、授权警告、安全检查、联系表、逐帧预览和真实透明桌宠启停已通过 104/104 回归及 macOS 隔离实测。
- [ ] Phase 6.3c/6.3d：标准包/候选构建的工作台任务化接入与真实验证。
- [ ] Phase 6.4–6.6：后台任务恢复、Kimi 视觉收口和最终可运行候选。

商业发布体系仍暂缓；Phase 6 不包含签名、公证、商城、支付、DRM、自动更新或正式发布。
