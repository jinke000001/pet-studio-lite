# Pet Studio Lite — 产品说明

> 当前产品的权威定义。旧产品 nom 的文档已归档到 `docs/legacy/`，不再代表现行需求。

## 一句话定位

**Pet Studio Lite 是一个本地优先的桌宠制作台：下载或导入 Petdex 宠物包 → 检查 → 预览 → 配置 → 导出离线 Windows x64 便携桌宠 ZIP。**

## 用户与场景

- 想从 Petdex 下载宠物并把它变成 Windows 桌宠的用户；
- 不想手动执行终端命令、寻找隐藏目录和来回选择文件的普通用户。

## 核心流程（五步）

1. **导入**：粘贴完整 Petdex 安装命令，下载并确认后导入；也支持目录或 ZIP。所有来源只读复制进制作台工作区，绝不修改原始包。
2. **检查**：结构、图集尺寸、可解码性、路径安全的完整校验，中文错误可理解。
3. **预览**：动作逐行预览 + 真实透明桌宠窗口预览（与导出运行时同一套代码）。
4. **配置**：显示名称 / 默认缩放（Windows 100% 屏幕实测默认 200%）/ 闲置游走开关。
5. **导出**：生成不依赖开发环境的 Windows x64 便携 ZIP，附 manifest 与启动说明。

## 授权模型

- 原包授权状态（`sourceLicense`）如实记录：`authorized` / `internal-test` / `unknown`（未声明），绝不伪造；
- 项目使用方式（`usageMode`）单独记录：未声明授权的包自动进入「个人／内部体验」；
- 最终产物性质（`distribution`）：仅 `authorized` + 常规使用生成 `candidate`，其余一律 `internal-test-only`，并在启动说明中明确「不得对外分发」。

## 边界

- 本地优先、无账号、无遥测；仅在用户主动执行 Petdex 命令导入时联网，并只读取对应的 `~/.petdex/pets/<slug>`；
- 不修改用户原始宠物包；不覆盖已有导出产物；
- macOS 上遵循 Mac 习惯：关窗口不退出，点 Dock 重开制作台；
- 界面跟随系统浅色/深色外观，无手动主题开关。

技术细节见 `CLAUDE.md`；验收记录见 `ACCEPTANCE.md`。
